# BareWire E2E Test Scenarios

> Generated by e2e-test-forge on 2026-03-28
> Test project: tests/BareWire.E2ETests
> Total scenarios: 14 (5 critical, 5 high, 4 medium)

## Happy Path (7 scenarios)

- **E2E-001** [critical] BasicPublishConsume: POST /messages → poll → GET /messages → message in DB
- **E2E-002** [critical] RequestResponse: POST /validate-order → synchronous OrderValidationResult
- **E2E-003** [critical] SagaOrderFlow happy path: POST /orders → simulate/pay → simulate/ship → status=Completed
- **E2E-004** [critical] TransactionalOutbox: POST /transfers → poll → GET /transfers → status=Completed
- **E2E-005** [high] RawMessageInterop: POST /legacy/simulate → GET /messages → message processed
- **E2E-006** [high] InboxDeduplication: POST /payments → POST /payments/duplicate → GET /notifications → only 1 notification per consumer
- **E2E-007** [high] ObservabilityShowcase: POST /demo/run → 3-hop cascade order→payment→shipment

## Error Handling (4 scenarios)

- **E2E-008** [critical] RetryAndDlq: POST /payments → retries exhausted → GET /payments/failed → message in DLQ
- **E2E-009** [high] SagaOrderFlow compensation: POST /orders → simulate/fail → simulate/compensate → status=Failed
- **E2E-010** [medium] RequestResponse invalid request: POST /validate-order with invalid data → error/invalid result
- **E2E-011** [high] InboxDeduplication redelivery: POST /payments/redeliver → GET /inbox → processed entry

## Edge Cases (3 scenarios)

- **E2E-012** [high] MultiConsumerPartitioning: POST /events/generate → GET /processing-log → sequential per CorrelationId
- **E2E-013** [medium] BackpressureDemo: POST /load-test/start?rate=5000 → GET /metrics → metrics active, no message loss
- **E2E-014** [medium] TransactionalOutbox pending: POST /transfers → GET /outbox/pending → count ≥ 0
